import {useState} from 'react';

const NetworkPanel = (props) => {
  const [state, setState] = useState()

  return (
    <div>
      Component NetworkPanel is loaded
    </div>
  );
}

export default NetworkPanel;