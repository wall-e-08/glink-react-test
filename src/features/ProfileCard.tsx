import {useState} from 'react';
import RoundedDiv from "../components/RoundedDiv";

const ProfileCard = (props) => {
  const [state, setState] = useState()

  return (
    <RoundedDiv>
      Component ProfileCard is loaded
    </RoundedDiv>
  );
}

export default ProfileCard;