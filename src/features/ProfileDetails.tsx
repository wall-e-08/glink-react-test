import {useState} from 'react';
import RoundedDiv from "../components/RoundedDiv";

const ProfileDetails = (props) => {
  const [state, setState] = useState()

  return (
    <RoundedDiv>
      Component ProfileDetails is loaded
    </RoundedDiv>
  );
}

export default ProfileDetails;