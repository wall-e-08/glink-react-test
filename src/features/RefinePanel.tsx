import {useState} from 'react';
import RoundedDiv from "../components/RoundedDiv";

const RefinePanel = (props) => {
  const [state, setState] = useState()

  return (
    <RoundedDiv>
      Component RefinePanel is loaded
    </RoundedDiv>
  );
}

export default RefinePanel;