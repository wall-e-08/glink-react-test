import {useState} from 'react';

const Topbar = (props) => {
  const [state, setState] = useState()

  return (
    <div>
      Component Navbar is loaded
    </div>
  );
}

export default Topbar;